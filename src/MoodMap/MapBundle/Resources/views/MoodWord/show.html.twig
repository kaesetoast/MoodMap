<h1>MoodWord</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Word</th>
            <td>{{ entity.word }}</td>
        </tr>
        <tr>
            <th>Colors</th>
            <td>{{ entity.colors }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_moodword') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_moodword_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('admin_moodword_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
