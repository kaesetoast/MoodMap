<h1>MoodWord list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Word</th>
            <th>Colors</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_moodword_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.word }}</td>
            <td>{{ entity.colors }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_moodword_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_moodword_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_moodword_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
